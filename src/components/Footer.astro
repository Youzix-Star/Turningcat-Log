<footer>
  <div class="footer-content">
    <p>
      <span class="material-symbols-outlined">favorite</span>
      Turningcat &copy; {new Date().getFullYear()}
    </p>
    <p class="links">
      <a href="https://github.com/Youzix-Star/Turningcat-Log" target="_blank">
        <span class="material-symbols-outlined">code</span>
        GitHub
      </a>
      <span class="divider">·</span>
      <a href="/rss.xml">
        <span class="material-symbols-outlined">rss_feed</span>
        RSS
      </a>
    </p>
  </div>
</footer>

<style>
  footer {
    margin-top: auto;
    background: var(--bg-glass);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    border-top: var(--glass-border);
  }

  .footer-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
    text-align: center;
    color: var(--text-muted);
    font-size: 0.875rem;
  }

  .footer-content p {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.375rem;
  }

  .footer-content .material-symbols-outlined {
    font-size: 1rem;
    color: #ef4444;
  }

  .links {
    margin-top: 0.75rem;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.75rem;
  }

  .links a {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    color: var(--text-muted);
    transition: color 0.2s ease;
  }

  .links a:hover {
    color: var(--primary);
  }

  .links a .material-symbols-outlined {
    font-size: 0.875rem;
    color: inherit;
  }

  .divider {
    color: var(--text-muted);
    opacity: 0.5;
  }
</style>