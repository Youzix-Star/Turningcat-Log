---
const currentPath = Astro.url.pathname;
---

<header>
  <nav>
    <a href="/" class="logo">
      <span class="material-symbols-outlined icon-filled">pets</span>
      <span class="logo-text">Turningcat Log</span>
    </a>
    <div class="nav-links">
      <a href="/" class:list={[{ active: currentPath === '/' }]}>
        <span class="material-symbols-outlined">home</span>
        <span>首页</span>
      </a>
      <button class="theme-toggle" id="themeToggle" aria-label="切换深色模式">
        <span class="material-symbols-outlined theme-icon-light">light_mode</span>
        <span class="material-symbols-outlined theme-icon-dark">dark_mode</span>
      </button>
      <a href="https://t.me/Turningcat_bot" target="_blank" class="bot-link" title="Telegram Bot">
        <span class="material-symbols-outlined">smart_toy</span>
      </a>
    </div>
  </nav>
</header>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const themeToggle = document.getElementById('themeToggle');
    const html = document.documentElement;
    
    const savedTheme = localStorage.getItem('theme') || 'light';
    if (savedTheme === 'dark') {
      html.classList.add('dark');
    }
    
    themeToggle.addEventListener('click', () => {
      html.classList.toggle('dark');
      const isDark = html.classList.contains('dark');
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
    });
  });
</script>

<style>
  header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--bg-glass);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    border-bottom: var(--glass-border);
    box-shadow: var(--shadow);
    transition: background 0.3s ease, border-color 0.3s ease;
  }

  nav {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0.875rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--text);
    letter-spacing: -0.02em;
  }

  .logo:hover {
    color: var(--primary);
  }

  .logo .material-symbols-outlined {
    font-size: 1.5rem;
    color: var(--primary);
  }

  .nav-links {
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  .nav-links a:not(.bot-link) {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    color: var(--text-secondary);
    font-weight: 500;
    font-size: 0.9rem;
    padding: 0.5rem 0.875rem;
    border-radius: 8px;
    transition: all 0.2s ease;
  }

  .nav-links a:not(.bot-link):hover,
  .nav-links a:not(.bot-link).active {
    color: var(--primary);
    background: rgba(37, 99, 235, 0.08);
  }

  .nav-links a:not(.bot-link) .material-symbols-outlined {
    font-size: 1.125rem;
  }

  /* 主题切换按钮 */
  .theme-toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: 10px;
    cursor: pointer;
    transition: all 0.2s ease;
    position: relative;
    overflow: hidden;
  }

  .theme-toggle:hover {
    background: var(--primary);
    border-color: var(--primary);
    transform: scale(1.05);
  }

  .theme-toggle:hover .material-symbols-outlined {
    color: white;
  }

  .theme-toggle .material-symbols-outlined {
    font-size: 1.25rem;
    color: var(--text-secondary);
    transition: color 0.2s ease;
    position: absolute;
  }

  .theme-icon-light {
    opacity: 1;
    transform: rotate(0deg) scale(1);
    transition: opacity 0.3s ease, transform 0.3s ease;
  }

  .theme-icon-dark {
    opacity: 0;
    transform: rotate(-90deg) scale(0.5);
    transition: opacity 0.3s ease, transform 0.3s ease;
  }

  :root.dark .theme-icon-light {
    opacity: 0;
    transform: rotate(90deg) scale(0.5);
  }

  :root.dark .theme-icon-dark {
    opacity: 1;
    transform: rotate(0deg) scale(1);
  }

  :root.dark .theme-toggle {
    background: rgba(59, 130, 246, 0.15);
    border-color: rgba(59, 130, 246, 0.3);
  }

  :root.dark .theme-toggle:hover {
    background: var(--primary);
  }

  /* Bot 按钮 - 最右边，只显示图标 */
  .bot-link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background: var(--primary);
    color: white !important;
    border-radius: 10px;
    box-shadow: var(--shadow);
    transition: all 0.2s ease !important;
  }

  .bot-link:hover {
    background: var(--primary-dark);
    box-shadow: var(--shadow-md);
    transform: translateY(-2px);
  }

  .bot-link .material-symbols-outlined {
    font-size: 1.25rem;
  }

  @media (max-width: 640px) {
    nav {
      padding: 0.75rem 1rem;
    }

    .nav-links a:not(.bot-link) span:not(.material-symbols-outlined) {
      display: none;
    }
  }
</style>