---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const logs = await getCollection('logs');
  return logs.map(log => ({
    params: { slug: log.slug },
    props: { log },
  }));
}

const { log } = Astro.props;
const { Content } = await log.render();

const formatDate = (d: Date) => {
  return d.toLocaleDateString('zh-CN', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    weekday: 'long',
  });
};

const getSourceName = () => {
  if (log.data.channelName) return log.data.channelName;
  if (!log.data.source) return '未知频道';
  const match = log.data.source.match(/t\.me\/([^/]+)/);
  return match ? `@${match[1]}` : '频道';
};
---

<BaseLayout title={`${log.data.title} - Turningcat Log`}>
  <Header />

  <main>
    <article class="log-article">
      <div class="article-header">
        <a href="/" class="back-link">
          <span class="material-symbols-outlined">arrow_back</span>
          <span>返回列表</span>
        </a>

        <div class="channel-section">
          {log.data.channelAvatar ? (
            <img src={log.data.channelAvatar} alt={log.data.channelName || '频道'} class="channel-avatar" />
          ) : (
            <div class="channel-avatar-placeholder">
              <span class="material-symbols-outlined">group</span>
            </div>
          )}
          <div class="channel-info">
            <span class="channel-name">{getSourceName()}</span>
            {log.data.channelUsername && (
              <a href={`https://t.me/${log.data.channelUsername}`} target="_blank" rel="noopener noreferrer" class="channel-link">
                @{log.data.channelUsername}
                <span class="material-symbols-outlined">open_in_new</span>
              </a>
            )}
          </div>
        </div>

        <h1>{log.data.title}</h1>

        <div class="meta">
          <div class="meta-item">
            <span class="material-symbols-outlined">calendar_today</span>
            <time datetime={log.data.date.toISOString()}>
              {formatDate(log.data.date)}
            </time>
          </div>

          {log.data.source && (
            <a href={log.data.source} target="_blank" rel="noopener noreferrer" class="meta-item source-link">
              <span class="material-symbols-outlined">link</span>
              <span>查看原文</span>
            </a>
          )}

          <div class="badges">
            {log.data.translated && (
              <span class="badge translated">
                <span class="material-symbols-outlined">translate</span>
                已翻译
              </span>
            )}
            {log.data.format && (
              <span class="badge format">
                <span class="material-symbols-outlined">description</span>
                {log.data.format.toUpperCase()}
              </span>
            )}
          </div>
        </div>
      </div>

      <div class="article-content">
        <Content />
      </div>

      <div class="article-footer">
        <a href="/" class="back-link">
          <span class="material-symbols-outlined">arrow_back</span>
          <span>返回列表</span>
        </a>
      </div>
    </article>
  </main>

  <Footer />
</BaseLayout>

<style>
  main {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem;
    min-height: calc(100vh - 200px);
  }

  .log-article {
    background: var(--bg-card);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    border: var(--glass-border);
    border-radius: 20px;
    overflow: hidden;
    box-shadow: var(--shadow);
  }

  .article-header {
    padding: 2rem;
    border-bottom: var(--glass-border);
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-bottom: 1.5rem;
    padding: 0.375rem 0.75rem;
    border-radius: 8px;
    transition: all 0.2s ease;
  }

  .back-link:hover {
    color: var(--primary);
    background: rgba(37, 99, 235, 0.08);
  }

  .back-link .material-symbols-outlined {
    font-size: 1.125rem;
  }

  .channel-section {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
    padding: 1rem;
    background: var(--bg-secondary);
    border-radius: 12px;
  }

  .channel-avatar {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    object-fit: cover;
    box-shadow: var(--shadow-md);
    border: 3px solid white;
  }

  .channel-avatar-placeholder {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-md);
  }

  .channel-avatar-placeholder .material-symbols-outlined {
    font-size: 1.5rem;
    color: white;
  }

  .channel-info {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .channel-name {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--text);
  }

  .channel-link {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    color: var(--text-muted);
    font-size: 0.85rem;
    transition: color 0.2s ease;
  }

  .channel-link:hover {
    color: var(--primary);
  }

  .channel-link .material-symbols-outlined {
    font-size: 0.875rem;
  }

  h1 {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 1.25rem;
    line-height: 1.4;
    letter-spacing: -0.02em;
  }

  .meta {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    align-items: center;
  }

  .meta-item {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    color: var(--text-muted);
    font-size: 0.875rem;
  }

  .meta-item .material-symbols-outlined {
    font-size: 1rem;
  }

  .source-link {
    transition: color 0.2s ease;
  }

  .source-link:hover {
    color: var(--accent);
  }

  .badges {
    display: flex;
    gap: 0.5rem;
    margin-left: auto;
  }

  .badge {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.375rem 0.75rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 500;
  }

  .badge .material-symbols-outlined {
    font-size: 0.875rem;
  }

  .badge.translated {
    background: rgba(14, 165, 233, 0.1);
    color: var(--accent);
  }

  .badge.format {
    background: rgba(37, 99, 235, 0.1);
    color: var(--primary);
  }

  .article-content {
    padding: 2rem;
    line-height: 1.8;
  }

  .article-content :global(h2) {
    font-size: 1.375rem;
    font-weight: 600;
    margin: 2rem 0 1rem;
    padding-bottom: 0.5rem;
    border-bottom: var(--glass-border);
    letter-spacing: -0.01em;
  }

  .article-content :global(h3) {
    font-size: 1.125rem;
    font-weight: 600;
    margin: 1.5rem 0 0.75rem;
  }

  .article-content :global(p) {
    margin-bottom: 1rem;
    color: var(--text);
  }

  .article-content :global(blockquote) {
    border-left: 3px solid var(--primary);
    padding: 0.75rem 1rem;
    margin: 1rem 0;
    background: rgba(37, 99, 235, 0.05);
    border-radius: 0 8px 8px 0;
    color: var(--text-secondary);
  }

  .article-content :global(ul),
  .article-content :global(ol) {
    margin: 1rem 0;
    padding-left: 1.5rem;
  }

  .article-content :global(li) {
    margin-bottom: 0.5rem;
  }

  .article-content :global(a) {
    color: var(--primary);
    text-decoration: underline;
    text-decoration-color: rgba(37, 99, 235, 0.3);
    text-underline-offset: 2px;
    transition: text-decoration-color 0.2s ease;
  }

  .article-content :global(a:hover) {
    text-decoration-color: var(--primary);
  }

  .article-footer {
    padding: 1.5rem 2rem;
    border-top: var(--glass-border);
  }

  @media (max-width: 640px) {
    main {
      padding: 1rem;
    }

    .article-header,
    .article-content,
    .article-footer {
      padding: 1.5rem;
    }

    .channel-section {
      padding: 0.75rem;
    }

    .channel-avatar,
    .channel-avatar-placeholder {
      width: 44px;
      height: 44px;
    }

    .channel-avatar-placeholder .material-symbols-outlined {
      font-size: 1.25rem;
    }

    h1 {
      font-size: 1.375rem;
    }

    .meta {
      flex-direction: column;
      align-items: flex-start;
    }

    .badges {
      margin-left: 0;
    }
  }
</style>